{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
 <main class="register row">
    <h1 class="text-center my-4">Créez votre compte</h1>
    <div class="col-5 mr-5">
    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.firstname) }}
        {{ form_row(registrationForm.lastname) }}
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.plainPassword, {
            label: 'Password'
        }) }}

        <button type="submit" class="btn btn-dark">Enregistrez-vous</button>
    {{ form_end(registrationForm) }}
    </div>
    <div class="col-4 ml-5 float-end">
      <img class="loginlogo mt-0 "src="{{ asset('doc/img/block/bl(4).png') }}" alt="block Not logo" style="max-width: 100%"></a>
    </div>
 </main>

{% endblock %}
