{% extends 'base.html.twig' %}

{% block title %}Modifier{% endblock %}

{% block body %}
    <h1 class="my-3">Modifier projet : " {{task.title}} "</h1>


    

     {{ form_start(form) }}
        {{ form_row(form.beginning) }}
        {{ form_row(form.end) }}
        {{ form_row(form.title) }}
        {{ form_row(form.content) }}
        {{ form_row(form.Modifier) }}
     {{ form_end(form) }}


        
    <a class="btn btn-outline-dark" href="{{ path('projects_index') }}">back to list</a>

    {{ include('projects/_delete_form.html.twig') }}
{% endblock %}

